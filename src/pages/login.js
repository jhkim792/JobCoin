import React, {Component} from 'react';
import GeminiCard from '../components/gemini-card'

export default class Login extends Component {
  render() {
    return ( < GeminiCard type = "login" / > )
  }
}
